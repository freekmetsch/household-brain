// Bistro — colour as seasoning. Atelier's engraved drawings in warm near-black
// ink, with exactly one terracotta accent reserved for each icon's detail
// paths (steam, ticks, seeds, scores). The quietest possible colour move.

import { repaintAtelier } from './recolor';

export const BISTRO = repaintAtelier({
	body: '#2e2a26',
	detail: '#c6572f'
});
